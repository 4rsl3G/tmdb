<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Admin • <%= siteName %></title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">

  <script src="https://cdn.jsdelivr.net/npm/jquery@3.7.1/dist/jquery.min.js"></script>
  <link rel="stylesheet" href="/ui.css"/>
</head>
<body>
  <div class="bg"><div class="bg-noise" aria-hidden="true"></div></div>

  <header class="topbar">
    <div class="container topbar-inner">
      <a class="brand" href="<%= adminPath %>">
        <span class="brand-mark pulse"></span>
        <span class="brand-name">Admin</span>
      </a>
      <nav class="nav">
        <a class="nav-link" href="<%= adminPath %>">Dashboard</a>
        <a class="nav-link" href="<%= adminPath %>/posts">Posts</a>
        <a class="nav-link" href="<%= adminPath %>/settings">Settings</a>
        <a class="nav-link" href="/" target="_blank" rel="noopener">Public</a>
      </nav>

      <form method="post" action="<%= adminPath %>/logout">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <button class="btn ghost" type="submit">Logout</button>
      </form>
    </div>
  </header>

  <main class="container">
    <%- body %>
  </main>

  <script src="/ui.js"></script>
</body>
</html>
